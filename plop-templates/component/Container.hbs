import { {{pascalCase name}}ContainerProps } from './{{pascalCase name}}.types';
import { {{pascalCase name}} } from './{{pascalCase name}}';

export const {{pascalCase name}}Container = ({}: {{pascalCase name}}ContainerProps) => {
  return <{{pascalCase name}} />;
};
